// Typewriter Effectconst roles = [  "Frontend Developer specializing in clean, performant, and accessible web interfaces.",  "I am a Developer.",  "I am a Designer.",  "I am a Problem Solver."];let roleIndex = 0;let charIndex = 0;const typeEl = document.getElementById("typewriter");function typeEffect() {  if (charIndex < roles[roleIndex].length) {    typeEl.textContent += roles[roleIndex].charAt(charIndex);    charIndex++;    setTimeout(typeEffect, 60);  } else {    setTimeout(eraseEffect, 1500);  }}function eraseEffect() {  if (charIndex > 0) {    typeEl.textContent = roles[roleIndex].substring(0, charIndex - 1);    charIndex--;    setTimeout(eraseEffect, 40);  } else {    roleIndex = (roleIndex + 1) % roles.length;    setTimeout(typeEffect, 500);  }}typeEffect();// Scroll Revealconst reveals = document.querySelectorAll(".reveal");function revealOnScroll() {  reveals.forEach(el => {    const rect = el.getBoundingClientRect();    if (rect.top < window.innerHeight - 100) {      el.classList.add("show");    }  });}window.addEventListener("scroll", revealOnScroll);revealOnScroll();// Theme Toggleconst toggleBtn = document.getElementById("themeToggle");let darkMode = true;toggleBtn.addEventListener("click", () => {  darkMode = !darkMode;  if (!darkMode) {    document.body.style.background = "#F8FAFC";    document.body.style.color = "#0F172A";    toggleBtn.textContent = "??";  } else {    document.body.style.background = "#0F172A";    document.body.style.color = "#F8FAFC";    toggleBtn.textContent = "??";  }});// Smooth Scrollfunction scrollToSection(id) {  document.getElementById(id).scrollIntoView({ behavior: "smooth" });}// Availability Badge (JS-powered)const availabilityEl = document.getElementById("availability");availabilityEl.textContent = "?? Available for new projects";